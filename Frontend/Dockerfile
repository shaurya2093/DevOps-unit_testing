# Use the official Nginx image from Docker Hub
FROM nginx:alpine

# Copy all your frontend files (like index.html, styles.css, etc.) into the default directory where Nginx looks for files
COPY ./ /usr/share/nginx/html

# Expose port 80 to make the website accessible
EXPOSE 80
